# ansible/roles/common/handlers/main.yml

- name: Restart SSH
  become: true
  ansible.builtin.service:
    name: ssh
    state: restarted  

- name: restart nginx
  ansible.builtin.systemd:
    name: nginx
    state: restarted
